node_modules/
dist/
.env
.env.local
.env.*.local
*.local

# Android build artifacts
android/.gradle/
android/build/
android/app/build/
*.apk
*.aab
*.keystore
*.jks
# Keystore de debug fixo — necessário para que atualizações do APK
# instalem por cima da versão anterior (mesma assinatura em todo build)
!android/app/debug.keystore
